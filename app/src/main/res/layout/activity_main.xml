<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".ui.MainActivity">

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerView"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        tools:listitem="@layout/item_note"
        android:overScrollMode="never"
        android:layoutAnimation="@anim/recyclerview_animation"/>

  <com.google.android.material.floatingactionbutton.FloatingActionButton
      android:id="@+id/fabAddNote"
      android:layout_width="wrap_content"
      android:layout_height="wrap_content"
      app:layout_constraintBottom_toBottomOf="parent"
      app:layout_constraintRight_toRightOf="parent"
      android:layout_margin="15dp"
      android:src="@drawable/ic_add"
      android:backgroundTint="@color/colorPrimary"
      app:tint="#FFFFFF"/>

    <View
        android:id="@+id/circleAnim"
        android:layout_width="64dp"
        android:layout_height="64dp"
        android:visibility="invisible"
        android:background="@drawable/fab_anim_shape"
        app:layout_constraintLeft_toLeftOf="@id/fabAddNote"
        app:layout_constraintRight_toRightOf="@id/fabAddNote"
        app:layout_constraintBottom_toBottomOf="@id/fabAddNote"/>

</androidx.constraintlayout.widget.ConstraintLayout>